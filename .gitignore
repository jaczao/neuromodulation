# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# macOS
.DS_Store
.claude/
__pycache__/
*.pyc

# Extra
extra/

# Local retest cache: trained net + pickled observer state (regenerable; pickles bind to module layout)
results/*_ckpt_*.pt
# Local retest cache for driver_traces (regenerable; pickles bind to module layout)
driver_traces/ckpt_*.pt

driver_traces/ckpt_sn_*.pt

# Per-SYNAPSE gate dumps (pt5_taskil): 9 MB each, ~81 MB total, and regenerable by re-running the
# study that wrote them. Same rule as the retest checkpoints above; the per-NEURON dumps (20 KB) are
# tracked, and every reported number lives in the .md/.log/.tsv regardless.
pt5_taskil/gate_alpha_*synapse*.npz
